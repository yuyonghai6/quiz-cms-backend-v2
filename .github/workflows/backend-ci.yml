name: CI/CD - Quiz CMS Backend

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build backend and run tests
        run: mvn clean package

      - name: Upload test reports if tests fail
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            **/target/surefire-reports/
            **/target/allure-results/

      - name: Upload orchestration JAR
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: orchestration-layer/target/*.jar

  # Deployment job - configure this based on your deployment target
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Download backend JAR
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: backend-jar
  #         path: ./deploy
  #
  #     # Example: Deploy to remote server via SSH
  #     # - name: Deploy to server
  #     #   uses: appleboy/scp-action@v0.1.4
  #     #   with:
  #     #     host: ${{ secrets.DEPLOY_HOST }}
  #     #     username: ${{ secrets.DEPLOY_USER }}
  #     #     key: ${{ secrets.DEPLOY_KEY }}
  #     #     source: "./deploy/*.jar"
  #     #     target: "/opt/app/"
  #     #
  #     # Example: Deploy to Docker registry
  #     # - name: Build and push Docker image
  #     #   uses: docker/build-push-action@v5
  #     #   with:
  #     #     context: .
  #     #     push: true
  #     #     tags: your-registry/quiz-cms-backend:latest
